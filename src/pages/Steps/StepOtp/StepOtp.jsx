import React from 'react'

const StepOtp = ({onNext}) => {
    return (
        <>
            <div>OTP Page</div>
            <button onClick={onNext}></button>
        </>
    )
}

export default StepOtp
